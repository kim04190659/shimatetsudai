# NotebookLM 学習資料｜「Notionとは何か」〜意思決定支援プラットフォームをNotionだけで作った理由

*パートナー向け説明用の一次資料。NotebookLMにそのままインポートして、この後に作成する「Claude」編・「実際のオペレーション」編とあわせて使う想定。*

---

## 1. Notionとは何か

Notionは、もともと「ドキュメント・タスク管理・データベースを1つのアプリに統合したオールインワン・ワークスペース」として知られるツールである。特徴は次の3層構造を持つこと。

1. **ページ(Page)**：文章・画像・埋め込みなど、自由形式のコンテンツを置く場所。Wordやブログ記事に近い。
2. **データベース(Database / Data Source)**：Excelの表のように、行(レコード)と列(プロパティ)を持つ構造化データ。列にはテキストだけでなく、選択肢(Select/Multi-select)・日付・チェックボックス・メールアドレス・URL・他のページへのリレーションなどの「型」を持たせられる。
3. **ブロック(Block)**：見出し・箇条書き・表・埋め込みなど、ページの中身を構成する最小単位。ページもデータベースの1行も、内部的にはブロックの集合として扱われる。

この「自由形式のページ」と「型を持った構造化データベース」が同じアプリの中で自然に行き来できる点が、Notionの最大の特徴である。議事録(自由形式)から、次のアクション(構造化データの1行)を作る、といったことがドラッグ&ドロップに近い感覚でできる。

さらに2024年以降、NotionはAPIとMCP(Model Context Protocol)対応を強化しており、外部のAIエージェント(Claudeなど)が、Notionのページ・データベースを「検索して読む」「新しいページ・行を作る」「データベースのスキーマ(列の構成)そのものを変更する」といった操作を、対話の中から直接実行できるようになっている。2026年時点では、Notion公式のホスト型MCPサーバーが検索・ページCRUD・データベース作成・コメント・ユーザー管理など18種類のツールを提供しており、データベース作成・更新の処理効率も大きく改善されている（[Notion Developer Platform 2026年5月アップデート](https://www.notion.com/releases/2026-05-13)）。

参考：
- [Notion MCP vs Notion AI: What to Use and Why (2026)](https://www.gend.co/blog/notion-mcp-vs-notion-ai)
- [Notion MCP Complete Guide — 6 Pitfalls of the Official Server & 2026 New API Features](https://kansei-link.com/en/insights/notion-mcp-deep-dive-2026)

---

## 2. Notionが他のAIツールと何が違うか

ChatGPTやClaude.aiのような「チャット単体のAIツール」、あるいはSlack・GoogleドキュメントのようなSaaSと比較したときの、Notionならではの違いは次の3点に整理できる。

### (1) 「会話の結果」がその場で構造化データとして残る

チャットAIとの会話は、基本的には流れて消えていく「テキストのやり取り」である。一方Notionでは、AIとのやり取りの結果を、あらかじめ設計した列(プロパティ)を持つデータベースの1行として保存できる。たとえば「住民の意見」を、ただの文章としてではなく、〈論点〉〈立場〉〈発言チャネル〉〈記録日時〉という列を持った1レコードとして残せるため、後から「論点ごとに絞り込む」「立場別に集計する」といった、表計算的な分析にそのままつなげられる。

### (2) 誰でも編集できる「共同編集の器」であること

構造化データベースというと、通常はエンジニアがSQLやプログラムで作るものだが、Notionのデータベースは非エンジニアでも画面上でスプレッドシート感覚で列を追加・編集できる。今回のプロジェクトでは、20〜40代の現地スタッフ(非エンジニア)が、Notion上で直接データを見て、住民や首長・議会と話をしながらレポートを作成する、という運用を前提にしている。エンジニアが作った成果物を「納品して終わり」にせず、現場の人がそのまま触り続けられる点が、他のAIツール・自社開発システムとの大きな違いである。

### (3) AIエージェントが「読む」だけでなく「書く・作る」ことができる

MCP経由でClaudeのようなAIエージェントがNotionに接続すると、単に既存ページを要約する(読む)だけでなく、新しいデータベースをその場で設計して作成する、既存データベースのスキーマに新しい列を追加する、既存ページを丸ごと複製して別拠点用のひな形を作る、複数ページ・複数データベースを横断してリンク集ページを自動生成する、といった「書く・作る」操作をエージェントに任せられる。今回のプロジェクトでは、これを使って1つの自治体向けに作った「合意形成プラットフォーム」(8つのデータベースのセット)を、Claudeへの指示だけで別の自治体向けに複製し、数時間で稼働させることができた。

---

## 3. なぜ「Notionだけ」を使って意思決定支援プラットフォームを作ったか

このプロジェクトでは、専用のWebアプリやデータベースサーバーをゼロから作るのではなく、意思決定支援プラットフォームのデータ層をあえてNotionだけで構築した。理由は次の4つである。

### 理由1：現地スタッフがサーバーやコードを触らずに運用できる

意思決定支援プラットフォームは、〈ステークホルダー〉〈論点(イシュー)〉〈立場の記録〉〈ステークホルダーの目標〉〈合意事項〉〈外部連携先〉〈オープンデータ〉〈根拠記録〉という8種類のデータベースで構成されている。これをすべてNotion上に作ったことで、現地スタッフはブラウザでNotionを開くだけで、最新の論点一覧やステークホルダーの状況を確認・追記できる。専用の管理画面を新しく開発・保守する必要がない。

### 理由2：スキーマ変更が「コードの変更・デプロイ」を必要としない

通常のシステム開発では、「データベースに新しい列を1つ追加する」だけでも、コードの修正・テスト・デプロイという一連の作業が発生する。Notionでは、AIエージェントへの指示1つで列を追加でき（例：ある自治体名を選択肢に追加する、対象自治体を複数選択できるようにする）、即座に運用に反映できる。開発のスピードと、現場からの「こういう情報も残したい」という要望への追従性が両立する。

### 理由3：「複数の島(自治体)」へ横展開する際の型として機能する

最初の自治体向けに作った8データベースの構成を、Notionの「ページ複製」機能でそのまま複製し、対象自治体名などの選択肢だけを調整すれば、2つ目・3つ目の自治体向けの環境をほぼ同じ手順で立ち上げられる。これは、コードとデータベースが密結合したシステムでは実現しづらい、Notionならではの「型の横展開のしやすさ」である。

### 理由4：Claude(AIエージェント)との相性が良い

ClaudeはMCP経由でNotionのデータベースを直接検索・集計・更新できるため、「このデータベースの中から、直近1週間で追加された論点だけを教えて」「このデータベースに新しい列を追加して」といった作業を、人がSQLやAPIコードを書かずに、対話形式でAIに依頼できる。意思決定支援に必要な「データを整理する」「レポートにまとめる」という作業そのものを、Notion＋Claudeの組み合わせで、非エンジニアでも回せる体制にできた。

---

## 4. 開発のポイント（実際にやったこと・つまずいたこと）

パートナー向けに「実際のオペレーション」を見せる際に強調したい、具体的な開発上のポイントを以下にまとめる。

### 4-1. 8データベースの「型」設計

意思決定支援に必要な情報を、以下の8つのデータベースに分解して設計した。

| データベース名 | 役割 |
|---|---|
| Stakeholder | 関係者(住民・議会・役場職員など)の一覧 |
| Issue | 議論すべき論点(イシュー) |
| PositionRecord | 論点に対する立場・意見の記録 |
| StakeholderGoal | 関係者ごとの目標・望む状態 |
| Agreement | 合意に至った事項 |
| ExternalPartner | 外部連携先(支援団体・報道など) |
| OpenDataSource | 参照した公開統計データ |
| EvidenceRecord | 意思決定の根拠となる記録 |

この型を最初の自治体で作り込んだことで、2つ目の自治体への横展開が「複製＋一部の選択肢調整」で済むようになった。

### 4-2. 「ページ複製」機能の落とし穴と、その学び

Notionには既存ページ(データベース一式を含む)をまるごと複製する機能がある。これを「まっさらな入れ物ができる」と誤解して使ったところ、複製元の自治体のデータ(行そのもの)まで一緒に複製されてしまうという問題が発生した。実際には、複製したデータベースから複製元のデータだけを見つけて、少しずつ安全に退避させるという、人の目による確認作業が必要になった。

この経験は、「AIによる効率化(複製という操作そのもの)」と「人にしかできない最終確認(本当に混ざっていないかのチェック)」の境界を、身をもって体験する結果になった。これは意思決定支援プラットフォームで扱っている論点「どの業務を効率化・共同化してよく、どの業務は人にしかできない仕事として残すべきか」と直接つながる、実例として説明しやすいエピソードである。

### 4-3. 属性(プロパティ)によるデータの絞り込み設計

複数の自治体・複数のアプリ(会社サイトの問い合わせチャット、住民向けアプリ「てつだって拡張版」など)から同じNotionワークスペースに書き込む構成のため、「どの自治体向けのお知らせか」「対象自治体はどこか」を選択肢(Select / Multi-select)のプロパティとして持たせ、AIエージェント側のクエリで絞り込む設計にした。これにより、1つの通知機能を複数自治体で使い回しながら、住民には自分の自治体に関係する情報だけを届けられるようにしている。

### 4-4. 「データベースのリンク集ページ」による運用の見える化

プロジェクトが進むにつれてデータベースの数が増え、どのデータベースがどこにあるか分かりにくくなったため、Notion上に「データベース一覧」ページを作成し、各データベースの役割・書き込み元のアプリ・環境変数名をまとめた。さらに自治体ごとに独立したリンク集ページも用意し、現地スタッフがブラウザのお気に入りに登録するだけで、自分の自治体に関係するデータベースにすぐアクセスできるようにした。

---

## 5. 成果物として何を生成できるか

Notion＋Claudeの組み合わせによって、このプロジェクトでは以下のような成果物をコードを書かずに(あるいは最小限のコードで)生成できている。

- **構造化データベース一式**：自治体ごとの合意形成プラットフォーム(8データベース)を、対話だけで設計・作成・複製できる。
- **データベースのスキーマ変更**：新しい自治体の追加、新しい選択肢の追加、新しい列の追加を、コード修正・デプロイなしに反映できる。
- **リンク集ページ・運用ドキュメント**：関連データベースへのリンクをまとめたページ、新しい拠点をオンボーディングする際の手順書(ランブック)を、既存の運用実態から自動的に文章化できる。
- **住民の意見・記録の蓄積**：しまのみんな会議(カード形式の意見収集アプリ)やてつだって拡張版(住民アプリ)から送られてくる意見・記録が、構造化データとしてNotionに蓄積され、あとから論点別・自治体別に分析できる。
- **レポート・記事の下書き**：蓄積されたデータや開発記録をもとに、Notion上でレポートやnote記事の下書きを、Claudeとの対話から直接作成できる。

これらはいずれも、「Notionという1つのデータ基盤の上で、AIエージェントが読み書きする」という共通の仕組みから生まれている。次のNotebookLM資料では、この読み書きを担っている「Claude」側の役割・仕組みについて説明する。

---

*作成：2026-08-16 / 吉孝さん × Claude*
