# NotebookLM 学習資料｜「Claudeとは何か」〜意思決定支援プラットフォームをClaudeと一緒に作った理由

*パートナー向け説明用の一次資料。「Notion編」の続編にあたる。この資料単体でも読めるように、Notion編と重なる前提部分もあらためて書いている。*

---

## 1. Claudeとは何か

ClaudeはAnthropic社が開発している対話型AI(大規模言語モデル)である。文章の生成・要約・分析といった「会話」の能力に加えて、このプロジェクトで中心的に使っているのは次の3つの利用形態である。

1. **チャットとしてのClaude**：Webやデスクトップアプリ上で、人が直接対話して質問・依頼をする使い方。文章の下書き、企画のブレインストーミング、コードの相談など。
2. **エージェントとしてのClaude(Claude Code / Cowork)**：ファイルの読み書き、コマンドの実行、外部サービスへのAPI呼び出しなど、実際に「手を動かす」作業までを任せられる形態。本プロジェクトで使っているのはこちら側で、コードの作成・ビルド・GitHubへの反映・Notion上のデータベース操作などを、対話の延長として実行している。
3. **アプリケーションに組み込まれたClaude(API経由)**：会社サイトの問い合わせチャット、住民アプリの写真日記の一言コメント生成など、こちらの開発したアプリケーションの内部から、Anthropic API経由でClaudeを呼び出して使う形態。ユーザーからは「Claudeと直接話している」ようには見えないが、裏側の応答生成・要約・分類の処理をClaudeが担っている。

2026年時点でのClaudeの主なモデルはOpus 5・Sonnet 5・Haiku 4.5であり、用途に応じて「精度重視で複雑な設計判断をするタスク」にはOpus/Sonnet系、「大量の単純な分類・要約を高速に処理するタスク」にはHaiku系、というように使い分けられる。本プロジェクトでは、住民からの問い合わせの一次分類や、写真からの一言日記生成といった「即答が必要で、かつ定型的な処理」にHaiku系のモデルを使っている。

Claudeは**MCP(Model Context Protocol)**という共通規格を通じて、Notion・GitHub・Vercel・Supabase・Googleカレンダーなど、外部のさまざまなサービスに接続できる。これにより、Claudeは「言葉で答えるだけの存在」から「実際にサービス側のデータを読み書きする存在」へと役割が広がっている。

---

## 2. Claudeが他のAIツールと何が違うか

ChatGPTなど他の対話型AIと比較したときに、このプロジェクトで実際に活きているClaudeならではの特徴は次の3点である。

### (1) 「会話」と「実際の作業」がひと続きになっている

一般的なチャットAIは、コードやアイデアを「提案」するところまでで終わり、実際にファイルを作る・サーバーにデプロイする・データベースを更新する、といった作業は人が引き継いで行う必要がある。Claude Code / Coworkの形態では、Claude自身がファイルの作成・編集、ターミナルでのコマンド実行、Gitへのコミット、Notion・Vercelなど外部サービスへの操作までを、一つの対話の中で連続して実行できる。「相談して終わり」ではなく「相談したその場で動くものができる」という体験が、他のチャットAIとの一番大きな違いである。

### (2) 複数のツール・複数のリポジトリを横断して作業できる

本プロジェクトは、会社サイト(shimatetsudai)、住民アプリ(shimatetsudai-tetsudatte)、意見収集アプリ(shimatetsudai-issue-cardgame)という複数の独立したコードリポジトリと、Notion上の複数のデータベース、GitHub、Vercelという複数のサービスにまたがって成立している。Claudeは、これらすべてに横断的にアクセスしながら、「会社サイトに新しい分室ページを作る→住民アプリにお知らせバナーを出す設定を加える→Notion側にその設定用のデータベース列を追加する」といった、複数サービスにまたがる一連の変更を、1つの依頼に対してまとめて実行できる。

### (3) 「型として動くツール」を、対話から複製・横展開できる

一度作った仕組み(たとえば、ある自治体向けの意思決定支援プラットフォーム一式)を、「これと同じものを、別の自治体向けに作って」という自然言語の指示だけで、Notion側のデータベース複製、コード側の自治体切り替えロジックの追加、会社サイトへの新しいページ追加までを一括して行える。人が毎回同じ手順を手作業で繰り返す必要がなく、かつ、単純なコピー&ペーストでは対応できない「その自治体固有の調整」もあわせて行える点が、定型的な自動化ツールとの違いである。

---

## 3. なぜClaudeを使って意思決定支援プラットフォームを作ったか

### 理由1：非エンジニアの発想を、そのままコード・データベース設計に翻訳できる

このプロジェクトの発案者はエンジニアではあるが、35年前に情報工学を学んだのみで、日常的にコードを書く立場ではない。「役場の職員が減っていく中で、どの業務を人に残すべきかを議論するカードゲームを作りたい」といった、業務課題ベースの自然な日本語の依頼から、実際に動くデータベース設計・カードゲームのデータ・Webページのコードまでを、Claudeが翻訳して形にしている。専門的なプログラミング知識がなくても、「作りたいものの意図」さえ明確であれば形にできる、という体験そのものが、このプロジェクトの前提になっている。

### 理由2：Notionという「共通の器」の読み書きを、人手を介さずに任せられる

Notion編で述べた通り、このプロジェクトのデータ基盤はNotionに集約している。Claudeは、MCP経由でこのNotionワークスペースに直接接続しており、データベースの設計・作成・スキーマ変更・行の作成/更新/移動を、人が画面を操作することなく実行できる。「Notionという共同編集の器」と「Claudeという読み書きの実行役」が組み合わさることで、非エンジニアの現場スタッフが直接触れる場所(Notion)と、開発・自動化を担う場所(Claude)を、明確に役割分担できている。

### 理由3：一次対応をAIに任せ、人は「判断が必要なところ」だけに集中できる

会社サイトの問い合わせ対応では、Claudeがまず自動応答を試み、AIだけでは答えられない・エスカレーションが必要と判断した内容だけをNotionに記録して人に引き継ぐ、という二段構えの設計にしている。同様に、住民アプリの写真日記でも、AIがその場で一言コメントを生成し、人(支援者)は「異変に気づいたときだけ」介入する設計にしている。てつだって拡張版全体を貫く「AIが一次対応、人は二次対応」という設計思想を、実装レベルで一貫して支えているのがClaudeである。

### 理由4：意思決定の「材料」を作るところまでを、対話から一括して任せられる

意思決定支援ダッシュボードは、統計データや住民の意見データを、会議にそのまま持ち込める1枚のグラフ・シートにまとめる役割を担っている。この「データを集めて、意思決定の材料として整形する」という作業自体を、Claudeとの対話の中で、リサーチ・データ整理・HTML/ダッシュボードの実装まで一括して依頼できる。

---

## 4. 開発のポイント(実際にやったこと・つまずいたこと)

### 4-1. MCP経由でのNotion操作を、確認しながら段階的に進める

Claudeは、Notionのデータベースに対して、検索・ページ作成・スキーマ変更(列の追加や型変更)・複数ページの一括移動といった操作をMCP経由で実行できる。ただし、大量のデータを一括で移動・削除するような操作は、意図しない結果を招くリスクがあるため、Cowork側の安全機構によって、大きすぎる一括操作は自動的にブロックされることがある(実際に、76件のデータを一度に移動しようとしてブロックされた)。この場合は、人に確認を取ったうえで、少数件ずつに分割して段階的に実行する、という運用にしている。「AIに任せてよい範囲」と「人の承認を挟むべき範囲」の線引きを、実際の運用の中で体験しながら設計している。

### 4-2. 直接のAPI呼び出しによる「構造化された応答」の設計

会社サイトの問い合わせチャットや、写真日記の一言コメント生成では、Claudeに単に自由文で答えさせるのではなく、「この形式のJSONで必ず答えてください」という制約(ツール強制呼び出し)を付けたうえでAnthropic APIを呼び出している。これにより、Claudeの回答を「そのままアプリの画面に表示する文章」と「Notionに記録するための構造化データ(問い合わせ種別・エスカレーション理由・担当候補など)」に、確実に分離して受け取れるようにしている。

### 4-3. 複数リポジトリ・複数サービスにまたがる変更の一貫性維持

会社サイト・住民アプリ・意見収集アプリという3つの独立したコードベースと、Notion側のデータベース設計は、それぞれ別々に存在しているため、たとえば「新しい自治体を追加する」といった変更は、複数の場所に同じ情報(自治体名など)を手作業で反映する必要がある。この「どこを同期し忘れると壊れるか」を明確にドキュメント化し、新しい拠点をオンボーディングする際の手順書(ランブック)としてNotionにまとめている。

### 4-4. 開発環境の制約に対する現実的な対応

開発環境(サンドボックス)の制約により、外部フォント読み込みなど一部の処理がビルド時にエラーになることがあるが、これは実際の本番環境(Vercelなど、通常のネットワークアクセスができる環境)では発生しない、開発環境固有の制約であることを明確に切り分けて説明し、無理に「開発環境で直そうとする」ことを避けている。AIエージェントであっても、環境の限界を正直に認識し、それを踏まえた判断をすることが、実務での信頼につながっている。

---

## 5. 成果物として何を生成できるか

Claudeとの対話から、実際に以下のような成果物をこのプロジェクトで生成している。

- **動くアプリケーションのコード**：Next.jsベースの会社サイト・住民アプリ・意見収集アプリを、画面設計からAPI実装、Google認証の組み込みまで一貫して構築。
- **Notion上のデータベース設計・運用資料**：意思決定支援プラットフォームの8データベース設計、拠点ごとのリンク集ページ、オンボーディング手順書(ランブック)。
- **AIを組み込んだ機能そのもの**：問い合わせ自動応答、写真からの一言日記生成、AIが自動生成する行為提案(カード形式の議論支援)など、Claudeをアプリの内部に組み込んだ機能。
- **意思決定の材料**：統計データと住民意見を組み合わせた意思決定支援ダッシュボード、会議にそのまま使えるレポート。
- **振り返り・広報コンテンツ**：開発の経緯や気づきを言語化したnote記事の下書きなど、開発そのものの記録を発信用コンテンツに変換したもの。

Notion編で述べた「データを蓄積する器」としてのNotionと、この資料で述べた「読み書きし、判断し、形にする実行役」としてのClaudeが組み合わさることで、非エンジニアの発案から、実際に住民が使うアプリケーション、そして意思決定の場に持ち込める資料までを、一気通貫で作れる体制になっている。

---

*作成：2026-08-16 / 吉孝さん × Claude*
